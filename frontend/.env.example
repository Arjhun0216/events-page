# Leave blank for local dev (Vite proxies /api to the backend, see
# vite.config.js). In production, set this to your deployed backend's URL,
# e.g. https://api.phantasm.fest — no trailing slash.
VITE_API_BASE_URL=
